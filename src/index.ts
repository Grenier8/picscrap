import { scrapAllPages } from "./scraping/scrap";
import { loadBaseProducts } from "./scraping/base";

scrapAllPages();

// loadBaseProducts();

// Searcher.findProductsBetweenPages(Picslab.webpage, Apertura.webpage).then((result) => {
//     console.log(result);
// });
